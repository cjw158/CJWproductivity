# MindWall 项目理解文档

> 项目全面概览 - 为新成员和 AI 助手准备

生成日期：2024-12-07

---

## 📌 项目概述

**MindWall（心智墙）** 是一个现代化的桌面生产力应用，旨在提供无摩擦的任务管理、笔记记录和计划追踪体验。

### 核心理念

- **零摩擦捕获**：快速记录想法和任务，不打断工作流
- **可视化管理**：通过看板、日历、矩阵等多种视图管理任务
- **桌面融合**：将任务清单渲染到桌面壁纸，时刻提醒
- **专注助力**：提供专注模式和计时器，帮助保持专注

### 技术定位

- **桌面应用**：基于 Tauri 2.0 构建的跨平台桌面应用
- **现代前端**：React 18 + TypeScript + Vite 技术栈
- **本地优先**：数据存储在本地 SQLite，保护隐私
- **性能卓越**：Rust 后端 + 优化的 React 组件

---

## 🏗️ 技术架构

### 技术栈

| 层级 | 技术 | 版本 | 用途 |
|------|------|------|------|
| **桌面框架** | Tauri | v2.0 | Rust 后端 + WebView2 渲染 |
| **前端框架** | React | v18.3 | UI 组件和状态管理 |
| **开发语言** | TypeScript | v5.x | 类型安全的 JavaScript |
| **构建工具** | Vite | v5.x | 极速开发服务器和构建 |
| **样式方案** | Tailwind CSS | v3.x | 原子化 CSS |
| **UI 组件库** | shadcn/ui | - | 可定制的 React 组件 |
| **动画引擎** | Framer Motion | v11.x | 声明式动画 |
| **状态管理** | TanStack Query | v5.x | 服务端状态缓存 |
| **拖拽交互** | dnd-kit | v6.x | 现代化拖拽库 |
| **富文本编辑** | Tiptap | v3.x | 所见即所得编辑器 |
| **数据库** | SQLite | - | 本地数据持久化 |

### 架构模式

```
┌─────────────────────────────────────────────────┐
│           UI Layer (React Components)           │
│  - TasksView, PlansView, NotesLayout           │
│  - Header, DynamicIsland, QuickCapture         │
└─────────────────────────────────────────────────┘
                      ↕
┌─────────────────────────────────────────────────┐
│         Custom Hooks Layer (React Hooks)        │
│  - useTasks, usePlans, useNotes                │
│  - useSettings, useTheme, useEnergyMode        │
└─────────────────────────────────────────────────┘
                      ↕
┌─────────────────────────────────────────────────┐
│      Repository Layer (Data Access Layer)       │
│  - TaskRepository, PlanRepository              │
│  - NoteRepository, SettingsRepository          │
└─────────────────────────────────────────────────┘
                      ↕
┌─────────────────────────────────────────────────┐
│          Storage Layer (SQLite / Mock)          │
│  - SQLite Database (Tauri environment)         │
│  - In-memory Mock (Browser environment)        │
└─────────────────────────────────────────────────┘
```

---

## 📂 项目结构

```
CJWproductivity/
├── src/                          # 前端源代码
│   ├── components/               # React 组件
│   │   ├── ui/                  # 基础 UI 组件（shadcn/ui）
│   │   ├── settings/            # 设置相关组件
│   │   ├── Header.tsx           # 顶部导航栏
│   │   ├── TasksView.tsx        # 任务视图容器
│   │   ├── PlansView.tsx        # 计划图片画廊
│   │   ├── NotesLayout.tsx      # 笔记布局
│   │   ├── QuickCapture.tsx     # 快速捕获面板
│   │   ├── DynamicIsland.tsx    # 灵动岛组件
│   │   ├── KanbanBoard.tsx      # 看板视图
│   │   ├── CalendarView.tsx     # 日历视图
│   │   ├── TimeBlockView.tsx    # 时间块视图
│   │   └── Dashboard.tsx        # 矩阵视图
│   ├── hooks/                    # 自定义 Hooks
│   │   ├── useTasks.ts          # 任务数据操作
│   │   ├── useNotes.ts          # 笔记数据操作
│   │   ├── usePlanImages.ts     # 计划图片操作
│   │   ├── useSettings.ts       # 设置管理
│   │   └── useTheme.ts          # 主题切换
│   ├── services/                 # 数据访问层（Repository）
│   │   ├── task/                # 任务仓储
│   │   ├── plan/                # 计划仓储
│   │   ├── planImage/           # 计划图片仓储
│   │   ├── note/                # 笔记仓储
│   │   └── settings/            # 设置仓储
│   ├── contexts/                 # React Context
│   │   └── ThemeContext.tsx     # 主题上下文
│   ├── lib/                      # 核心工具库
│   │   ├── database.ts          # 数据库初始化
│   │   ├── tasks.ts             # 任务辅助函数
│   │   ├── notes.ts             # 笔记辅助函数
│   │   ├── backup.ts            # 数据备份
│   │   ├── island.ts            # 灵动岛控制
│   │   ├── globalShortcuts.ts   # 全局快捷键
│   │   └── logger.ts            # 日志工具
│   ├── utils/                    # 纯工具函数
│   │   ├── index.ts             # 通用工具
│   │   ├── date.ts              # 日期处理
│   │   └── task.ts              # 任务工具
│   ├── types/                    # TypeScript 类型定义
│   │   ├── index.ts             # 主要类型
│   │   └── settings.ts          # 设置类型
│   ├── config/                   # 常量配置
│   │   └── constants.ts         # 全局常量
│   ├── App.tsx                   # 应用入口组件
│   ├── main.tsx                  # React 挂载点
│   └── index.css                 # 全局样式
├── src-tauri/                    # Tauri 后端（Rust）
│   ├── src/
│   │   └── main.rs              # Rust 入口文件
│   ├── tauri.conf.json          # Tauri 配置
│   └── Cargo.toml               # Rust 依赖
├── public/                       # 静态资源
│   └── logo.svg                 # 应用 Logo
├── docs/                         # 文档目录
│   ├── PLAN_FEATURE_GUIDE.md    # 计划功能指南
│   ├── PROJECT_UNDERSTANDING.md # 本文档
│   └── TODO.md                  # 开发计划
├── README.md                     # 项目说明
├── AI_GUIDE.md                   # AI 助手指南
├── CHANGELOG.md                  # 更新日志
├── package.json                  # npm 依赖配置
├── tsconfig.json                 # TypeScript 配置
├── tailwind.config.js            # Tailwind CSS 配置
└── vite.config.ts                # Vite 构建配置
```

---

## 🎯 核心功能模块

### 1. 任务管理系统

**文件位置**：`src/components/TasksView.tsx` 及相关组件

**主要功能**：
- ✅ **看板视图**（KanbanBoard）：四列工作流（收件箱 → 待办 → 执行中 → 已完成）
- ✅ **日历视图**（CalendarView）：月历总览，查看任务分布
- ✅ **时间块视图**（TimeBlockView）：TickTick 风格的时间轴日视图
- ✅ **矩阵视图**（Dashboard）：艾森豪威尔四象限管理
- ✅ **WIP 限制**：执行中最多 3 个任务
- ✅ **批量操作**：Shift + 点击多选
- ✅ **右键菜单**：快捷操作（完成/删除/移动）

**数据模型**：
```typescript
interface Task {
  id: number;
  content: string;
  status: "INBOX" | "TODO" | "DOING" | "DONE";
  due_date: string | null;
  scheduled_time: string | null;
  duration: number | null;
  created_at: string;
  completed_at: string | null;
}
```

### 2. 计划管理系统

**文件位置**：`src/components/PlansView.tsx` + `src/services/plan/`

**主要功能**：
- ✅ **计划 CRUD**：创建、读取、更新、删除计划
- ✅ **关键结果**：OKR 风格的目标和关键结果管理
- ✅ **进度追踪**：自动计算计划完成度
- ✅ **图片画廊**：上传和管理手写计划图片
- ✅ **瀑布流布局**：响应式多列展示
- ✅ **Lightbox 预览**：全屏预览、缩放、旋转

**数据模型**：
```typescript
interface Plan {
  id: number;
  title: string;
  description: string | null;
  color: string;
  progress: number;
  status: "active" | "completed" | "archived";
  start_date: string | null;
  end_date: string | null;
  created_at: string;
  updated_at: string;
}

interface KeyResult {
  id: number;
  plan_id: number;
  title: string;
  target_value: number;
  current_value: number;
  unit: string;
  progress: number;
  created_at: string;
  updated_at: string;
}
```

**详细文档**：参见 [PLAN_FEATURE_GUIDE.md](./PLAN_FEATURE_GUIDE.md)

### 3. 笔记系统

**文件位置**：`src/components/NotesLayout.tsx` + `src/components/RichTextEditor.tsx`

**主要功能**：
- ✅ 富文本编辑器（基于 Tiptap）
- ✅ Markdown 支持
- ✅ 笔记置顶
- ✅ 笔记 CRUD 操作

**数据模型**：
```typescript
interface Note {
  id: number;
  content: string;
  is_pinned: boolean;
  created_at: string;
  updated_at: string | null;
}
```

### 4. 快速捕获

**文件位置**：`src/components/QuickCapture.tsx`

**主要功能**：
- ✅ Smart Capture：自然语言日期识别（基于 chrono-node）
- ✅ 快捷键唤起：`F` + `J` 全局快捷键
- ✅ 双模式切换：按 `Tab` 在笔记/任务模式间切换
- ✅ 快速关闭：按 `Enter` 保存并关闭

### 5. 灵动岛

**文件位置**：`src/components/DynamicIsland.tsx` + `src/lib/island.ts`

**主要功能**：
- ✅ 桌面悬浮窗显示当前任务
- ✅ 可拖拽移动
- ✅ 专注模式计时器
- ✅ 计时结束闪烁提醒

### 6. 专注模式

**文件位置**：`src/components/FocusOverlay.tsx`

**主要功能**：
- ✅ 全屏专注模式
- ✅ 番茄钟计时器
- ✅ 任务倒计时
- ✅ 背景音乐（可选）

### 7. 设置系统

**文件位置**：`src/components/settings/SettingsModal.tsx`

**主要功能**：
- ✅ 通用设置（语言、启动页）
- ✅ 快捷键设置
- ✅ 外观设置（主题、字体大小）
- ✅ 开发者设置（动态壁纸、调试）
- ✅ 数据备份与恢复

---

## 🎨 设计系统

### 主题系统

**两种主题**：
1. **霓虹暗黑**（Neon Dark）
   - 深色背景（#000000, #1a1a1f）
   - 霓虹色彩强调（青色、紫色、绿色）
   - 发光边框效果
   - 适合夜间使用

2. **Apple 浅色**（Apple Light）
   - 浅色背景（#F2F2F7, #FFFFFF）
   - 柔和色彩（蓝色、灰色为主）
   - 参考 iOS 设计语言
   - 适合白天使用

**主题切换**：
- 通过 Header 右上角的太阳/月亮图标切换
- 使用 CSS 变量和 Tailwind CSS 实现
- 切换时立即生效，无延迟

### 颜色规范

**暗黑模式**：
```css
--neon-cyan: #22D3EE;      /* 青色强调 */
--neon-magenta: #E879F9;   /* 紫色强调 */
--neon-lime: #A3E635;      /* 绿色强调 */
--bg-dark: #000000;        /* 背景 */
--bg-card: #1a1a1f;        /* 卡片背景 */
```

**浅色模式**：
```css
--color-blue: #007AFF;     /* 蓝色强调 */
--color-purple: #AF52DE;   /* 紫色强调 */
--bg-light: #F2F2F7;       /* 背景 */
--bg-card: #FFFFFF;        /* 卡片背景 */
```

### 动画规范

- **进入动画**：`opacity: 0 → 1`, `y: 20 → 0`, 时长 `0.3s`
- **退出动画**：`opacity: 1 → 0`, `y: 0 → -20`, 时长 `0.2s`
- **悬停效果**：`scale: 1 → 1.02`, `shadow: none → 2xl`
- **弹性动画**：使用 `spring` 物理效果

---

## 🔄 数据流

### Repository 模式

```
Component (UI)
    ↓ 调用 Hook
Custom Hook (useTasks, usePlans)
    ↓ 使用 TanStack Query
Repository Interface (ITaskRepository)
    ↓ 实现
Concrete Repository (TaskRepository)
    ↓ 检测环境
SQLite (Tauri) or Mock (Browser)
```

**优点**：
- 业务逻辑与存储实现解耦
- 易于测试（可以 Mock Repository）
- 支持多种存储方式

### 状态管理

**TanStack Query**：
- 用于服务端状态缓存
- 自动重试和错误处理
- 乐观更新支持

**React Context**：
- 用于全局 UI 状态（主题、设置）
- 避免 prop drilling

**本地状态**：
- 使用 `useState` 管理组件内状态
- 使用 `useRef` 管理不触发渲染的状态

---

## 🚀 开发工作流

### 安装依赖

```bash
npm install
```

### 开发模式

```bash
# 前端开发（浏览器预览，使用 Mock 数据）
npm run dev

# Tauri 开发（桌面应用，使用 SQLite）
npm run tauri dev
```

### 构建生产版本

```bash
# 构建桌面应用
npm run tauri build
```

### 代码规范

- **TypeScript**：启用严格模式，避免 `any` 类型
- **ESLint**：待配置
- **Prettier**：待配置
- **命名规范**：
  - 组件：PascalCase (`TasksView`)
  - 函数：camelCase (`createTask`)
  - 常量：UPPER_SNAKE_CASE (`MAX_WIP_LIMIT`)
  - 类型：PascalCase (`Task`, `Plan`)

---

## 📚 关键概念

### Repository 模式

**定义**：将数据访问逻辑封装在独立的 Repository 类中，业务逻辑通过接口与 Repository 交互。

**实现**：
```typescript
// 接口定义
interface ITaskRepository {
  getAllTasks(): Promise<Task[]>;
  createTask(input: CreateTaskInput): Promise<Task>;
  // ...
}

// 具体实现
class TaskRepository implements ITaskRepository {
  async getAllTasks(): Promise<Task[]> {
    if (this.isMock) {
      return this.mockTasks;
    }
    return await this.db.select<Task[]>("SELECT * FROM tasks");
  }
  // ...
}
```

### Mock 数据策略

**目的**：在浏览器开发环境中，无法使用 SQLite，因此使用内存数据模拟数据库行为。

**实现**：
```typescript
class TaskRepository {
  private isMock = false;
  private mockTasks: Task[] = [];

  async initialize() {
    if (typeof window !== "undefined" && !("__TAURI__" in window)) {
      this.isMock = true;
    } else {
      this.db = await Database.load("sqlite:app.db");
    }
  }
}
```

### React Query

**目的**：管理服务端状态，自动缓存、重试、更新。

**使用**：
```typescript
// 查询数据
const { data: tasks, isLoading } = useQuery({
  queryKey: ["tasks"],
  queryFn: () => taskRepository.getAllTasks(),
});

// 修改数据
const createMutation = useMutation({
  mutationFn: (input: CreateTaskInput) => taskRepository.createTask(input),
  onSuccess: () => {
    queryClient.invalidateQueries({ queryKey: ["tasks"] });
  },
});
```

---

## 🔒 权限和安全

### Tauri 权限

在 `src-tauri/capabilities/default.json` 中配置：

```json
{
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "dialog:allow-save",
    "fs:allow-read",
    "fs:allow-write",
    "global-shortcut:allow-register",
    "sql:allow-load",
    "sql:allow-execute"
  ]
}
```

### 数据安全

- **本地存储**：数据存储在用户本地，不上传云端
- **无追踪**：不收集任何用户数据
- **SQLite 加密**：未来可以考虑使用 SQLCipher

---

## 🐛 已知问题

1. **计划图片**：
   - 浏览器 Mock 模式下无法持久化
   - 缺少缩略图生成
   - 数据备份不包含图片文件

2. **性能**：
   - 大量任务加载时可能卡顿
   - 图片画廊需要虚拟滚动优化

3. **兼容性**：
   - 移动端适配不完善
   - 部分快捷键在 macOS 上有冲突

---

## 📖 学习资源

### 官方文档

- [Tauri 文档](https://tauri.app/)
- [React 文档](https://react.dev/)
- [TypeScript 文档](https://www.typescriptlang.org/)
- [TanStack Query 文档](https://tanstack.com/query/latest)
- [Tailwind CSS 文档](https://tailwindcss.com/)

### 项目相关

- [README.md](./README.md) - 项目介绍
- [AI_GUIDE.md](./AI_GUIDE.md) - AI 助手指南
- [CHANGELOG.md](./CHANGELOG.md) - 更新日志
- [PLAN_FEATURE_GUIDE.md](./PLAN_FEATURE_GUIDE.md) - 计划功能详解
- [TODO.md](./TODO.md) - 开发计划

---

## 💡 开发建议

### 新功能开发流程

1. **理解需求**：明确功能目标和用户场景
2. **设计数据模型**：在 `src/types/index.ts` 定义类型
3. **实现 Repository**：在 `src/services/` 创建仓储
4. **创建 Hook**：在 `src/hooks/` 封装数据操作
5. **开发 UI**：在 `src/components/` 创建组件
6. **测试验证**：手动测试和自动化测试
7. **更新文档**：更新 CHANGELOG 和相关文档

### 代码审查要点

- [ ] 类型定义完整，无 `any` 类型
- [ ] 组件使用 `memo()` 优化
- [ ] 异步操作有错误处理
- [ ] 遵循命名规范
- [ ] 添加必要的注释
- [ ] 更新相关文档

---

## 🎯 未来展望

### v1.1（短期）
- 计划列表视图和 CRUD UI
- 关键结果管理界面
- 计划与任务关联

### v1.2（中期）
- 计划统计面板
- 进度图表可视化
- 时间线甘特图

### v2.0（长期）
- 多人协作
- 云同步
- 移动端 App
- AI 智能助手

---

## 📞 联系方式

- **项目主页**：https://github.com/cjw158/CJWproductivity
- **问题反馈**：提交 GitHub Issue
- **功能建议**：在 Discussions 中讨论

---

<p align="center">
  <b>感谢使用 MindWall！</b><br>
  <i>让生产力管理变得简单而优雅</i>
</p>
